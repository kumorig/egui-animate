[package]
name = "egui_animate"
version = "0.2.1"
edition = "2021"
description = "Custom animations and transitions for egui (local fork for egui 0.33)"
license = "MIT OR Apache-2.0"

[dependencies]
egui = { version = "=0.33.3", default-features = false }
